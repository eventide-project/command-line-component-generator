module <%= "#{name.camelize}Component" %>
  class Projection
    include EntityProjection
    include Messages::Events

    entity_name :<%= "#{name.downcase}" %>

    # TODO Implement event projection blocks
    # eg:
    # apply SomethingHappened do |something_happened|
    #   SetAttributes.(<%= "#{name.downcase}" %>, something_happened, copy: [
    #     { :<%= "#{name.downcase}" %>_id => :id }
    #   ])

    #   something_happened_time = Clock.parse(something_happened.time)

    #   <%= "#{name.downcase}" %>.something_happened_time = something_happened_time
    # end
  end
end
